name: Build and Release

on:
  # Trigger from rgb-lib repository
  repository_dispatch:
    types: [rgb-lib-release]

  # Manual trigger
  workflow_dispatch:
    inputs:
      rgb_lib_version:
        description: 'rgb-lib version to use (e.g., v0.3.0-beta.4)'
        required: true
        type: string

jobs:
  build:
    name: Build (${{ matrix.platform }}-${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x64
          - os: ubuntu-latest
            platform: linux
            arch: x64
            target: x86_64-unknown-linux-gnu
            lib_name: librgblibcffi.so
          # macOS ARM64 (Apple Silicon)
          - os: macos-latest
            platform: darwin
            arch: arm64
            target: aarch64-apple-darwin
            lib_name: librgblibcffi.dylib

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Get rgb-lib version
        id: rgb_lib_version
        run: |
          if [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            echo "version=${{ github.event.client_payload.version }}" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "version=${{ inputs.rgb_lib_version }}" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install SWIG
        run: |
          if [ "${{ matrix.platform }}" == "linux" ]; then
            sudo apt-get update
            sudo apt-get install -y swig
          elif [ "${{ matrix.platform }}" == "darwin" ]; then
            brew install swig
          fi
        shell: bash

      - name: Download rgb-lib c-ffi library
        run: |
          DOWNLOAD_URL="https://github.com/UTEXO-Protocol/rgb-lib/releases/download/${{ steps.rgb_lib_version.outputs.version }}/rgb-lib-cffi-${{ matrix.target }}.zip"
          echo "Downloading from: $DOWNLOAD_URL"
          curl -L -o rgb-lib-cffi.zip "$DOWNLOAD_URL"
          
          # Create directory structure
          mkdir -p rgb-lib/bindings/c-ffi/target/${{ matrix.target }}/release
          mkdir -p rgb-lib/bindings/c-ffi
          
          # Extract to temp and move files
          unzip -o rgb-lib-cffi.zip -d temp-cffi/
          
          # Move library files to release directory
          mv temp-cffi/*.so temp-cffi/*.dylib temp-cffi/*.a temp-cffi/*.lib temp-cffi/*.dll \
             rgb-lib/bindings/c-ffi/target/${{ matrix.target }}/release/ 2>/dev/null || true
          
          # Move header files to c-ffi directory
          mv temp-cffi/*.h temp-cffi/*.hpp rgb-lib/bindings/c-ffi/ 2>/dev/null || true
          
          rm -rf temp-cffi
          
          echo "=== Library files ==="
          ls -la rgb-lib/bindings/c-ffi/target/${{ matrix.target }}/release/
          echo "=== Header files ==="
          ls -la rgb-lib/bindings/c-ffi/
        shell: bash

      - name: Prepare platform package
        run: |
          PACKAGE_DIR="packages/rgb-lib-${{ matrix.platform }}-${{ matrix.arch }}"
          mkdir -p "$PACKAGE_DIR"
          
          # Copy template files
          cp template/binding.gyp "$PACKAGE_DIR/"
          cp template/package.json "$PACKAGE_DIR/"
          cp template/rgblib.js "$PACKAGE_DIR/"
          cp template/wrapper.js "$PACKAGE_DIR/"
          cp template/swig.i "$PACKAGE_DIR/"
          cp template/README.md "$PACKAGE_DIR/"
          
          # Determine library filename and extension
          if [ "${{ matrix.platform }}" == "linux" ]; then
            LIB_FILENAME="librgblibcffi"
            LIB_EXTENSION="so"
            OS_NAME="linux"
          elif [ "${{ matrix.platform }}" == "darwin" ]; then
            LIB_FILENAME="librgblibcffi"
            LIB_EXTENSION="dylib"
            OS_NAME="darwin"
          else
            LIB_FILENAME="rgblibcffi"
            LIB_EXTENSION="dll"
            OS_NAME="win32"
          fi
          
          # Update binding.gyp
          sed -i.bak "s/%TARGET%/${{ matrix.target }}/g" "$PACKAGE_DIR/binding.gyp"
          
          # Update package.json with all placeholders
          sed -i.bak "s/%PLATFORM%/${{ matrix.platform }}-${{ matrix.arch }}/g" "$PACKAGE_DIR/package.json"
          sed -i.bak "s/%OS%/$OS_NAME/g" "$PACKAGE_DIR/package.json"
          sed -i.bak "s/%CPU%/${{ matrix.arch }}/g" "$PACKAGE_DIR/package.json"
          # Remove 'v' prefix from version for npm
          NPM_VERSION=$(echo "${{ steps.rgb_lib_version.outputs.version }}" | sed 's/^v//')
          sed -i.bak "s/%VERSION%/$NPM_VERSION/g" "$PACKAGE_DIR/package.json"
          sed -i.bak "s/%TARGET%/${{ matrix.target }}/g" "$PACKAGE_DIR/package.json"
          sed -i.bak "s/%FILENAME%/$LIB_FILENAME/g" "$PACKAGE_DIR/package.json"
          sed -i.bak "s/%EXTENSION%/$LIB_EXTENSION/g" "$PACKAGE_DIR/package.json"
          
          # Remove backup files
          rm -f "$PACKAGE_DIR"/*.bak
          
          # Create c-ffi directory structure inside package
          mkdir -p "$PACKAGE_DIR/rgb-lib/bindings/c-ffi/target/${{ matrix.target }}/release"
          
          # Copy library files
          cp rgb-lib/bindings/c-ffi/target/${{ matrix.target }}/release/* "$PACKAGE_DIR/rgb-lib/bindings/c-ffi/target/${{ matrix.target }}/release/" 2>/dev/null || true
          
          # Copy header files to the expected location
          cp rgb-lib/bindings/c-ffi/rgblib.hpp "$PACKAGE_DIR/rgb-lib/bindings/c-ffi/" 2>/dev/null || true
          cp rgb-lib/bindings/c-ffi/rgblib.h "$PACKAGE_DIR/rgb-lib/bindings/c-ffi/" 2>/dev/null || true
          
          echo "=== Package structure ==="
          find "$PACKAGE_DIR" -type f
        shell: bash

      - name: Generate SWIG wrapper
        working-directory: packages/rgb-lib-${{ matrix.platform }}-${{ matrix.arch }}
        run: |
          swig -c++ -javascript -node swig.i
        shell: bash

      - name: Build native module
        working-directory: packages/rgb-lib-${{ matrix.platform }}-${{ matrix.arch }}
        run: |
          # Install node-gyp globally
          npm install -g node-gyp
          
          # Install package dependencies (node-addon-api, etc)
          npm install --ignore-scripts
          
          # Build native module
          node-gyp configure
          node-gyp build
        shell: bash

      - name: Package artifact
        run: |
          cd packages
          zip -r ../rgb-lib-nodejs-${{ matrix.platform }}-${{ matrix.arch }}.zip rgb-lib-${{ matrix.platform }}-${{ matrix.arch }}
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: rgb-lib-nodejs-${{ matrix.platform }}-${{ matrix.arch }}
          path: rgb-lib-nodejs-${{ matrix.platform }}-${{ matrix.arch }}.zip
          retention-days: 5

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version info
        id: version
        run: |
          if [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            VERSION="${{ github.event.client_payload.version }}"
          else
            VERSION="${{ inputs.rgb_lib_version }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
        shell: bash

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release-assets
          find artifacts -name "*.zip" -exec cp {} release-assets/ \;
          ls -la release-assets/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Release ${{ steps.version.outputs.version }}
          body: |
            ## Node.js bindings for rgb-lib
            
            Based on rgb-lib ${{ steps.version.outputs.version }}
            
            ### Installation
            
            Download the package for your platform and extract it.
            
            ### Platforms
            
            - Linux x64 (`linux-x64`)
            - macOS ARM64 / Apple Silicon (`darwin-arm64`)
            
            ### Changes
            
            See [rgb-lib release notes](https://github.com/UTEXO-Protocol/rgb-lib/releases/tag/${{ steps.version.outputs.version }})
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, 'alpha') || contains(steps.version.outputs.version, 'beta') || contains(steps.version.outputs.version, 'rc') }}
          files: release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

